apiVersion: v1
kind: ServiceAccount
metadata:
  name: mall-maintenance-sa
  namespace: opsera-mall-maintenance-dev
  labels:
    tenant: opsera
    app: mall-maintenance
    managed-by: opsera-c2c
  annotations:
    # IRSA annotation (if using IAM roles for service accounts)
    # eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/ROLE_NAME
