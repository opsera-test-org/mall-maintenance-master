apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml

# Use labels with includeSelectors: false (RULE 106)
labels:
  - pairs:
      app.kubernetes.io/name: mall-maintenance
      app.kubernetes.io/managed-by: opsera
    includeSelectors: false
